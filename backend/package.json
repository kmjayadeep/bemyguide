{
	"name": "bemyguide-backend",
	"scripts": {
		"dev": "wrangler dev --ip 0.0.0.0 --env dev",
		"deploy": "wrangler deploy --minify --env production",
		"cf-typegen": "wrangler types --env-interface CloudflareBindings"
	},
	"dependencies": {
		"hono": "^4.7.11"
	},
	"devDependencies": {
		"wrangler": "^4.20.0"
	}
}
